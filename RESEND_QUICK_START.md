# Быстрый старт с Resend

## ✅ Resend проще, чем Unisender!

### Шаг 1: Регистрация (2 минуты)

1. Зайдите на https://resend.com
2. Зарегистрируйтесь (можно через GitHub)
3. Скопируйте API ключ из раздела "API Keys"

### Шаг 2: Добавьте домен (5 минут)

1. В панели Resend → "Domains" → "Add Domain"
2. Введите ваш домен (например, `yourdomain.com`)
3. Добавьте DNS записи в ваш DNS провайдер:
   - SPF запись
   - DKIM записи (3 записи)
   - DMARC (опционально)

**Важно:** Без верифицированного домена Resend не будет работать!

### Шаг 3: Настройте .env

Добавьте в файл `.env`:

```env
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
RESEND_SENDER_EMAIL=noreply@yourdomain.com
RESEND_SENDER_NAME=HR Spasibo Bot
RESEND_ADMIN_EMAIL=admin@yourdomain.com
```

### Шаг 4: Перезапустите

```bash
# Перезапустите backend
sudo systemctl restart your-backend-service
# или
cd backend && python3 -m uvicorn app:app --reload
```

### Готово! 🎉

Теперь система будет использовать Resend для отправки email. **Не требуется подтверждение email** - письма отправляются сразу!

## Преимущества Resend

✅ **Проще** - один API запрос вместо сложной логики  
✅ **Быстрее** - письма отправляются сразу, без ожидания подтверждения  
✅ **Надежнее** - лучшая доставляемость, меньше попаданий в спам  
✅ **3,000 писем/месяц бесплатно** - достаточно для начала

## Проверка работы

После одобрения пользователя проверьте логи:

```
INFO: Используется Resend для отправки email
INFO: Отправка email через Resend на адрес: user@example.com
INFO: Email успешно отправлен на user@example.com, email_id: xxxxxx
```

Если видите эти сообщения - все работает! ✅

## Проблемы?

См. подробную инструкцию в `RESEND_SETUP.md`
