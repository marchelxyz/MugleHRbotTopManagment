# Руководство по оптимизации производительности

## 📋 Что нужно сделать ВРУЧНУЮ на сервере Railway

### 1. Настройка переменных окружения в Railway

В панели Railway для вашего проекта добавьте/проверьте следующие переменные:

```bash
# Для оптимизации пула соединений (уже настроено в коде, но можно переопределить)
DATABASE_POOL_SIZE=10          # Количество постоянных соединений (по умолчанию 10)
DATABASE_MAX_OVERFLOW=20       # Максимум дополнительных соединений (по умолчанию 20)

# Для production сервера
WORKERS=4                      # Количество worker процессов (рекомендуется: CPU cores * 2 + 1)
UVICORN_WORKERS=4              # Альтернативная переменная для uvicorn
```

**Как это сделать:**
1. Откройте ваш проект в Railway Dashboard
2. Перейдите в раздел "Variables"
3. Добавьте переменные выше
4. Перезапустите сервис

**⚠️ ВАЖНО:** Эти настройки влияют ТОЛЬКО на ваш проект. Другие проекты на Railway не будут затронуты, так как каждый проект имеет свои собственные переменные окружения и ресурсы.

### 2. Настройка ресурсов Railway (опционально)

Если вы испытываете проблемы с производительностью:

1. Откройте ваш проект в Railway Dashboard
2. Перейдите в настройки сервиса (Settings)
3. Увеличьте:
   - **CPU**: до 2-4 vCPU (если доступно)
   - **RAM**: до 2-4 GB (если доступно)

**Стоимость:** Railway взимает плату за использование ресурсов. Увеличение ресурсов увеличит стоимость.

**⚠️ ВАЖНО:** Увеличение ресурсов для одного проекта НЕ влияет на другие проекты. Каждый проект имеет выделенные ресурсы.

### 3. Настройка PostgreSQL на Railway (если есть доступ)

Если у вас есть доступ к настройкам PostgreSQL:

```sql
-- Оптимизация для production (выполните в psql или через Railway CLI)
ALTER SYSTEM SET shared_buffers = '256MB';
ALTER SYSTEM SET effective_cache_size = '1GB';
ALTER SYSTEM SET maintenance_work_mem = '128MB';
ALTER SYSTEM SET checkpoint_completion_target = 0.9;
ALTER SYSTEM SET wal_buffers = '16MB';
ALTER SYSTEM SET default_statistics_target = 100;
ALTER SYSTEM SET random_page_cost = 1.1;
ALTER SYSTEM SET effective_io_concurrency = 200;
ALTER SYSTEM SET work_mem = '4MB';
ALTER SYSTEM SET min_wal_size = '1GB';
ALTER SYSTEM SET max_wal_size = '4GB';
```

**Как это сделать:**
1. Откройте PostgreSQL сервис в Railway Dashboard
2. Перейдите в "Data" → "Query"
3. Выполните команды выше
4. Перезапустите PostgreSQL сервис

**⚠️ ВАЖНО:** Эти настройки влияют ТОЛЬКО на базу данных вашего проекта. Другие проекты используют свои собственные базы данных.

### 4. Настройка CDN для статики (опционально)

Если ваш фронтенд развернут на Vercel, CDN уже настроен автоматически.

Если используете другой хостинг:
- Настройте Cloudflare или другой CDN для статических файлов
- Включите сжатие gzip/brotli
- Настройте кэширование статики (CSS, JS, изображения)

## ✅ Что уже сделано автоматически в коде

### 1. Оптимизация пула соединений БД
- ✅ Настроен `pool_pre_ping=True` для проверки соединений
- ✅ Настроен `pool_recycle=1800` (30 минут)
- ✅ Добавлены настраиваемые `pool_size` и `max_overflow`

### 2. Production конфигурация сервера
- ✅ Создан файл `gunicorn_config.py` для production
- ✅ Настроены оптимальные параметры workers
- ✅ Добавлена поддержка переменных окружения

### 3. Индексы базы данных
- ✅ Создана миграция для добавления индексов на часто используемые поля
- ✅ Индексы на `telegram_id`, `timestamp`, `status` и другие

### 4. Оптимизация фронтенда
- ✅ Настроена оптимизация сборки Vite
- ✅ Включено сжатие и минификация
- ✅ Оптимизация chunk splitting

### 5. Middleware для сжатия
- ✅ Добавлен middleware для сжатия ответов (gzip)

## 🔒 Гарантии изоляции проектов на Railway

**Railway обеспечивает полную изоляцию проектов:**

1. **Изоляция ресурсов:** Каждый проект имеет выделенные CPU и RAM
2. **Изоляция переменных окружения:** Переменные одного проекта не видны другим
3. **Изоляция базы данных:** Каждый проект использует свою БД
4. **Изоляция сети:** Проекты не могут взаимодействовать друг с другом напрямую

**Вывод:** Оптимизации для этого проекта НЕ повлияют на другие проекты на вашем аккаунте Railway.

## 📊 Мониторинг производительности

### Метрики для отслеживания:

1. **Время ответа API:**
   - Целевое значение: < 200ms для простых запросов
   - Целевое значение: < 500ms для сложных запросов

2. **Использование памяти:**
   - Мониторинг через Railway Dashboard
   - Предупреждение при использовании > 80% RAM

3. **Использование CPU:**
   - Мониторинг через Railway Dashboard
   - Предупреждение при использовании > 70% CPU

4. **Соединения с БД:**
   - Мониторинг через логи приложения
   - Проверка на утечки соединений

## 🚀 Рекомендации по масштабированию

Если нагрузка растет:

1. **Горизонтальное масштабирование:**
   - Увеличьте количество workers (через переменную `WORKERS`)
   - Railway автоматически распределит нагрузку

2. **Вертикальное масштабирование:**
   - Увеличьте CPU и RAM в настройках Railway

3. **Оптимизация запросов:**
   - Используйте пагинацию для больших списков
   - Добавьте кэширование для часто запрашиваемых данных

4. **База данных:**
   - Рассмотрите использование read replicas для чтения
   - Оптимизируйте медленные запросы

## 📝 Чеклист для ручной настройки

- [ ] **ВАЖНО:** Применить миграцию индексов (см. ниже)
- [ ] Добавить переменные окружения в Railway (раздел 1)
- [ ] (Опционально) Увеличить ресурсы в Railway (раздел 2)
- [ ] (Опционально) Настроить PostgreSQL (раздел 3)
- [ ] (Опционально) Настроить CDN (раздел 4)
- [ ] Проверить метрики производительности после изменений

## 🗄️ Применение миграции индексов

**ОБЯЗАТЕЛЬНО:** Для улучшения производительности запросов нужно применить миграцию с индексами.

### ✅ Способ 1: Через API endpoint (САМЫЙ ПРОСТОЙ - РЕКОМЕНДУЕТСЯ)

Railway не имеет веб-интерфейса для SQL запросов, поэтому самый простой способ - использовать API endpoint:

```bash
curl -X POST https://your-api.railway.app/admin/apply-performance-indexes \
  -H "X-Admin-Key: YOUR_ADMIN_API_KEY"
```

Или через браузер/Postman:
- **URL:** `POST /admin/apply-performance-indexes`
- **Header:** `X-Admin-Key: ваш_админ_ключ`

**Преимущества:**
- ✅ Не требует дополнительных инструментов
- ✅ Можно запустить из любого места
- ✅ Безопасно (требует админ ключ)

### Способ 2: Через Python скрипт

```bash
# Через Railway CLI
railway run python3 backend/run_migrations_performance.py

# Или локально (если настроено подключение)
cd backend
python3 run_migrations_performance.py
```

### Способ 3: Через внешний PostgreSQL клиент

Если у вас есть доступ к строке подключения:
1. Используйте DBeaver, pgAdmin или psql
2. Подключитесь к базе данных Railway
3. Выполните SQL из `backend/migrations/006_add_performance_indexes.sql`

**📚 Подробные инструкции:** См. файл `RAILWAY_DB_MIGRATION_GUIDE.md`

**⚠️ ВАЖНО:** Миграция безопасна - она использует `CREATE INDEX IF NOT EXISTS`, поэтому можно запускать несколько раз без ошибок.

## 🔗 Полезные ссылки

- [Railway Documentation](https://docs.railway.app/)
- [FastAPI Deployment](https://fastapi.tiangolo.com/deployment/)
- [PostgreSQL Performance Tuning](https://wiki.postgresql.org/wiki/Performance_Optimization)
