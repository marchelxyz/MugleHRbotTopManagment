# Пошаговая инструкция: Добавление и верификация домена в Resend

## Шаг 1: Вход в панель Resend

1. Откройте https://resend.com
2. Войдите в свой аккаунт
3. В левом меню найдите раздел **"Domains"** и кликните на него

## Шаг 2: Добавление домена

1. Нажмите кнопку **"Add Domain"** (или **"Добавить домен"**)
2. Введите ваш домен в поле:
   - Например: `yourdomain.com` (без `www` и `http://`)
   - Или поддомен: `mail.yourdomain.com`
3. Нажмите **"Add"** или **"Добавить"**

## Шаг 3: Получение DNS записей

После добавления домена Resend покажет вам список DNS записей, которые нужно добавить:

### Пример записей, которые покажет Resend:

```
Type: TXT
Name: @
Value: v=spf1 include:_spf.resend.com ~all

Type: CNAME
Name: resend._domainkey
Value: resend._domainkey.resend.com

Type: CNAME
Name: resend2._domainkey
Value: resend2._domainkey.resend.com

Type: CNAME
Name: resend3._domainkey
Value: resend3._domainkey.resend.com

Type: TXT
Name: _dmarc
Value: v=DMARC1; p=none; rua=mailto:dmarc@resend.com
```

**Важно:** У вас будут свои уникальные значения! Скопируйте их из панели Resend.

## Шаг 4: Добавление DNS записей в ваш DNS провайдер

### Если используете Cloudflare:

1. Войдите в Cloudflare
2. Выберите ваш домен
3. Перейдите в раздел **"DNS"** → **"Records"**
4. Для каждой записи:
   - Нажмите **"Add record"**
   - Выберите тип (TXT или CNAME)
   - Введите имя (Name)
   - Введите значение (Value)
   - Нажмите **"Save"**

### Если используете другие DNS провайдеры:

**Для Namecheap:**
1. Войдите в аккаунт Namecheap
2. Domain List → Manage → Advanced DNS
3. Добавьте записи вручную

**Для GoDaddy:**
1. Войдите в аккаунт GoDaddy
2. My Products → DNS → Manage DNS
3. Добавьте записи вручную

**Для Яндекс.Почта для домена:**
1. Войдите в Яндекс.Коннект
2. Почта → Настройки → Почтовые домены
3. Выберите домен → Настройки DNS
4. Добавьте записи

**Для REG.RU, Timeweb, Beget и других:**
1. Войдите в панель управления
2. Найдите раздел "DNS" или "Управление DNS"
3. Добавьте записи вручную

## Шаг 5: Примеры добавления записей

### SPF запись (TXT):

```
Тип: TXT
Имя: @ (или оставьте пустым, или ваш домен)
Значение: v=spf1 include:_spf.resend.com ~all
TTL: 3600 (или Auto)
```

### DKIM записи (CNAME):

**Запись 1:**
```
Тип: CNAME
Имя: resend._domainkey
Значение: resend._domainkey.resend.com
TTL: 3600
```

**Запись 2:**
```
Тип: CNAME
Имя: resend2._domainkey
Значение: resend2._domainkey.resend.com
TTL: 3600
```

**Запись 3:**
```
Тип: CNAME
Имя: resend3._domainkey
Значение: resend3._domainkey.resend.com
TTL: 3600
```

### DMARC запись (TXT, опционально):

```
Тип: TXT
Имя: _dmarc
Значение: v=DMARC1; p=none; rua=mailto:dmarc@resend.com
TTL: 3600
```

## Шаг 6: Ожидание верификации

1. После добавления всех записей вернитесь в панель Resend
2. Нажмите кнопку **"Verify"** (или **"Проверить"**)
3. Обычно верификация занимает **5-30 минут**, но может занять до **24 часов**

**Примечание:** DNS записи распространяются не мгновенно. Подождите несколько минут после добавления.

## Шаг 7: Проверка статуса

В панели Resend вы увидите статус домена:

- ✅ **Verified** (Верифицирован) - домен готов к использованию
- ⏳ **Pending** (Ожидает) - записи еще не распространились
- ❌ **Failed** (Ошибка) - проверьте записи

## Частые проблемы и решения

### Проблема: "DNS records not found"

**Решение:**
1. Убедитесь, что вы добавили все записи правильно
2. Проверьте, что имя записи указано верно (для корневого домена может быть `@` или пустое)
3. Подождите 10-30 минут для распространения DNS

### Проблема: "Invalid CNAME record"

**Решение:**
1. Убедитесь, что значение CNAME заканчивается точкой (`.`) в некоторых DNS провайдерах
2. Проверьте, что имя записи указано полностью (например, `resend._domainkey.yourdomain.com`)

### Проблема: Долго не верифицируется

**Решение:**
1. Используйте онлайн-инструменты для проверки DNS:
   - https://mxtoolbox.com/SuperTool.aspx
   - https://dnschecker.org/
2. Введите ваш домен и проверьте, видны ли записи
3. Если записи не видны - подождите еще немного (DNS может распространяться до 24 часов)

## Альтернатива: Использование поддомена

Если у вас проблемы с верификацией основного домена, можно использовать поддомен:

1. Добавьте поддомен: `mail.yourdomain.com` или `email.yourdomain.com`
2. Добавьте те же DNS записи, но для поддомена
3. Используйте этот поддомен в настройках:
   ```env
   RESEND_SENDER_EMAIL=noreply@mail.yourdomain.com
   ```

## Проверка DNS записей через командную строку

После добавления записей можно проверить их через терминал:

```bash
# Проверка SPF записи
dig TXT yourdomain.com

# Проверка DKIM записей
dig CNAME resend._domainkey.yourdomain.com
dig CNAME resend2._domainkey.yourdomain.com
dig CNAME resend3._domainkey.yourdomain.com

# Проверка DMARC
dig TXT _dmarc.yourdomain.com
```

## После верификации

Когда домен будет верифицирован:

1. ✅ Вы сможете отправлять письма с этого домена
2. ✅ Письма будут иметь хорошую репутацию
3. ✅ Меньше вероятность попадания в спам

## Использование в приложении

После верификации домена используйте его в `.env`:

```env
RESEND_SENDER_EMAIL=noreply@yourdomain.com
```

Готово! Теперь можно отправлять письма через Resend. 🎉
