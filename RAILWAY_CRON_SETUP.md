# Настройка Railway Cron для автоматических задач

Этот документ описывает процесс настройки Railway Cron для замены GitHub Actions workflows.

## 📋 Обзор

Railway Cron позволяет запускать задачи по расписанию напрямую из Railway, что более надежно и удобно, чем использование GitHub Actions.

## 🚀 Настройка

### Шаг 1: Подготовка скриптов

Скрипты для запуска cron задач уже созданы:
- `backend/run_daily_cron.py` - для ежедневных задач
- `backend/run_monthly_cron.py` - для ежемесячных задач

### Шаг 2: Настройка переменных окружения в Railway

Убедитесь, что в вашем Railway проекте установлены следующие переменные окружения:

1. **ADMIN_API_KEY** - секретный ключ для авторизации cron запросов (должен совпадать с тем, что используется в основном сервисе)
2. **RAILWAY_SERVICE_URL** (рекомендуется) - Railway автоматически предоставляет эту переменную для внутренних запросов между сервисами. Это самый эффективный способ.
   
   **Альтернативно:**
   - **RAILWAY_PUBLIC_DOMAIN** - публичный домен вашего сервиса в Railway
   - **BACKEND_URL** - кастомный URL (например, `https://api.yourdomain.com`)

   **Важно:** Скрипты автоматически используют `RAILWAY_SERVICE_URL` если он доступен, что обеспечивает более быстрые и надежные внутренние запросы.

### Шаг 3: Создание Cron Jobs в Railway Dashboard

#### Для ежедневных задач:

1. Откройте Railway Dashboard
2. Перейдите в ваш проект
3. Нажмите "New" → "Cron Job"
4. Настройте следующие параметры:
   - **Name**: `daily-tasks`
   - **Schedule**: `1 0 * * *` (каждый день в 00:01 UTC)
   - **Command**: `python backend/run_daily_cron.py`
   - **Service**: Выберите ваш основной backend сервис (или создайте отдельный сервис для cron)

#### Для ежемесячных задач:

1. Создайте еще один Cron Job:
   - **Name**: `monthly-tasks`
   - **Schedule**: `5 0 1 * *` (1-го числа каждого месяца в 00:05 UTC)
   - **Command**: `python backend/run_monthly_cron.py`
   - **Service**: Тот же сервис, что и для daily-tasks

### Шаг 4: Проверка работы

После настройки:

1. Проверьте логи в Railway Dashboard для каждого cron job
2. Можно вручную запустить cron job для тестирования через Railway Dashboard
3. Убедитесь, что задачи выполняются успешно

## 📝 Расписание задач

### Ежедневные задачи (`/scheduler/run-daily-tasks`)
- **Расписание**: `1 0 * * *` (00:01 UTC каждый день)
- **Что выполняется**:
  - Начисление бонусов на день рождения
  - Сброс билетов для рулетки
  - Сброс дневных лимитов переводов

### Ежемесячные задачи (`/scheduler/run-monthly-tasks`)
- **Расписание**: `5 0 1 * *` (00:05 UTC 1-го числа каждого месяца)
- **Что выполняется**:
  - Генерация баннеров лидерборда за месяц

## 🔒 Безопасность

- Все cron запросы защищены через заголовок `X-Cron-Secret`
- Секретный ключ хранится в переменных окружения Railway
- Эндпоинты недоступны без правильного секретного ключа

## 🔄 Миграция с GitHub Actions

После успешной настройки Railway Cron:

1. Убедитесь, что Railway Cron работает стабильно (проверьте несколько запусков)
2. Можно отключить GitHub Actions workflows:
   - Удалите или закомментируйте файлы `.github/workflows/daily_tasks.yml` и `.github/workflows/monthly_tasks.yml`
   - Или отключите их в настройках GitHub репозитория

## 🐛 Отладка

Если cron задачи не выполняются:

1. **Проверьте логи в Railway Dashboard** - там будут видны все ошибки
2. **Проверьте переменные окружения** - убедитесь, что `ADMIN_API_KEY` и `BACKEND_URL` установлены правильно
3. **Проверьте доступность эндпоинта** - убедитесь, что основной сервис работает и доступен
4. **Проверьте формат расписания** - используйте формат cron: `минута час день месяц день_недели`

## 📚 Дополнительные ресурсы

- [Railway Cron Documentation](https://docs.railway.app/develop/cron)
- [Cron Expression Format](https://crontab.guru/)
